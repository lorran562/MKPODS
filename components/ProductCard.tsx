'use client';

import { useState } from 'react';
import Image from 'next/image';
import { ArrowUpRight, ShoppingBag } from 'lucide-react';
import { Product } from '@/lib/store';

interface ProductCardProps { product: Product; onBuy: (product: Product, flavor: string) => void; }

export default function ProductCard({ product, onBuy }: ProductCardProps) {
  const [selectedFlavor, setSelectedFlavor] = useState(product.flavors.find((flavor) => flavor.available)?.name || '');
  return <article className="group flex h-full flex-col overflow-hidden rounded-2xl border border-white/[0.08] bg-[#101414] transition duration-500 hover:-translate-y-1 hover:border-emerald-400/30 hover:shadow-[0_18px_45px_rgba(0,0,0,.25)] sm:rounded-[1.6rem]"><div className="relative aspect-square overflow-hidden bg-[#151b19] p-2.5 sm:p-4"><div className="relative h-full w-full overflow-hidden rounded-xl bg-[#0c100f] sm:rounded-[1.15rem]"><Image src={product.image} alt={product.model} fill className="object-cover transition duration-700 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-black/35 to-transparent" /><span className="absolute left-2 top-2 rounded-full border border-emerald-300/20 bg-emerald-300/10 px-2 py-1 text-[8px] font-bold uppercase tracking-wider text-emerald-300 sm:left-3 sm:top-3 sm:px-2.5 sm:text-[10px] sm:tracking-widest">{product.capacity}</span><span className="absolute bottom-2 left-2 rounded-full bg-black/50 px-2 py-1 text-[8px] font-bold uppercase tracking-wider text-white/70 backdrop-blur sm:bottom-3 sm:left-3 sm:px-2.5 sm:text-[10px] sm:tracking-widest">{product.brand}</span></div></div><div className="flex flex-grow flex-col p-3 sm:p-5"><div className="flex items-start justify-between gap-1.5"><div><h3 className="font-display text-xl font-bold tracking-[-.05em] text-white sm:text-2xl">{product.model}</h3><p className="mt-0.5 text-[9px] text-zinc-500 sm:mt-1 sm:text-xs">Perfil intenso · original</p></div><ArrowUpRight size={15} className="mt-1 shrink-0 text-zinc-600 transition group-hover:text-emerald-400 sm:h-[18px] sm:w-[18px]" /></div><div className="mt-3 sm:mt-5"><p className="mb-2 text-[8px] font-bold uppercase tracking-[.16em] text-zinc-600 sm:text-[10px] sm:tracking-[.2em]">Sabor</p><div className="flex flex-wrap gap-1">{product.flavors.map((flavor) => <button key={flavor.name} disabled={!flavor.available} onClick={() => setSelectedFlavor(flavor.name)} className={`min-h-7 rounded-full border px-2 py-1 text-[9px] font-medium transition sm:min-h-0 sm:px-2.5 sm:py-1.5 sm:text-[10px] ${!flavor.available ? 'cursor-not-allowed border-white/5 bg-white/[0.02] text-zinc-700' : selectedFlavor === flavor.name ? 'border-emerald-400 bg-emerald-400 text-[#07100b]' : 'border-white/10 bg-white/[0.03] text-zinc-400 hover:border-emerald-400/40 hover:text-white'}`}>{flavor.name}</button>)}</div></div><div className="mt-auto pt-4 sm:pt-6"><div className="mb-3 flex flex-col gap-1 sm:mb-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[8px] uppercase tracking-[.14em] text-zinc-600 sm:text-[10px] sm:tracking-[.18em]">Pix</p><strong className="font-display text-xl tracking-[-.04em] text-white sm:text-2xl">R$ {product.price}</strong></div><span className="text-[8px] font-bold uppercase tracking-widest text-emerald-400 sm:text-[10px]">Em estoque</span></div><button onClick={() => onBuy(product, selectedFlavor)} className="flex min-h-11 w-full items-center justify-center gap-1.5 rounded-xl bg-emerald-400 px-2 py-3 text-[11px] font-bold text-[#07100b] transition hover:bg-emerald-300 active:scale-[.98] sm:gap-2 sm:text-sm"><ShoppingBag size={14} /><span className="sm:hidden">Comprar</span><span className="hidden sm:inline">Adicionar ao carrinho</span></button></div></div></article>;
}

